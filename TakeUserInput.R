# 2.	Create an R Program to Take Input from the User.

# Take numeric input
num <- as.numeric(readline(prompt = "Enter a number: "))
print(paste("You entered:", num))

# Take string input
str <- readline(prompt = "Enter a string: ")
print(paste("You entered:", str))
